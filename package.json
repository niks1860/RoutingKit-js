{
  "name": "routing-kit",
  "version": "1.0.0",
  "description": "A wrapper around c++ RoutingKit library to find shortest paths between nodes in a graph",
  "main": "index.js",
  "scripts": {
    "install": "napa",
    "build:routing-kit": "docker run --rm -v $(pwd)/core:/src trzeci/emscripten ./build.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nikunj Dhorajiya",
  "license": "ISC",
  "napa": {
    "routing-kit": "git+https://github.com/RoutingKit/RoutingKit.git"
  },
  "devDependencies": {
    "napa": "^3.0.0"
  }
}
